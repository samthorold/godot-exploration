## Agent skills

### Issue tracker

Issues live as GitHub issues; use the `gh` CLI. (No git remote is configured yet — push the repo to GitHub before filing issues.) See `docs/agents/issue-tracker.md`.

### Triage labels

Canonical defaults (`needs-triage`, `needs-info`, `ready-for-agent`, `ready-for-human`, `wontfix`). See `docs/agents/triage-labels.md`.

### Domain docs

Single-context layout: `CONTEXT.md` + `docs/adr/` at the repo root. See `docs/agents/domain.md`.
